---
phase: 15
plan: 1
wave: 1
---

# Plan 15.1: Evolução do Schema de Banco e Versionamento (NIJA-CORE)

## Objective
Estabelecer a base de dados robusta para o NIJA-MAESTRO, garantindo que cada etapa do pipeline seja persistida e que documentos jurídicos possuam versionamento vinculado ao caso e ao cliente.

## Context
- .gsd/phases/integration_plan.md
- supabase/migrations/20260403163800_process_dossiers_and_judge_simulations.sql
- supabase/migrations/20260325140000_create_legal_documents_table.sql

## Tasks

<task type="auto">
  <name>Criar Migration de Integração NIJA</name>
  <files>
    - supabase/migrations/20260404020000_nija_integration_core.sql
  </files>
  <action>
    Criar uma nova migration que implemente:
    1. Tabela `nija_pipeline_runs`:
       - id, case_id, office_id, status (running, completed, failed), current_stage, logs (jsonb), started_at, finished_at.
    2. Tabela `nija_reviews`:
       - id, piece_id (uuid), quality_score (int), findings (jsonb), recommendation (text), created_at.
    3. Alteração em `legal_documents`:
       - Adicionar `case_id` (uuid), `client_id` (uuid), `version` (int), `parent_id` (uuid - para árvore de versões).
    4. Alteração em `judge_simulations`:
       - Garantir vínculo com `document_id`.
    5. Políticas RLS de isolamento por `office_id` em todas as novas tabelas.
  </action>
  <verify>Check table creation in Supabase logic</verify>
  <done>As tabelas e colunas citadas existem no banco e as políticas de RLS estão ativas.</done>
</task>

<task type="auto">
  <name>Atualizar View de Timeline Unificada</name>
  <files>
    - supabase/migrations/20260404021000_update_unified_timeline_nija.sql
  </files>
  <action>
    Atualizar a view `unified_client_events` para incluir:
    - Eventos de criação de Dossiê.
    - Eventos de conclusão de Auditoria (Reviews).
    - Eventos de Simulação de Juiz IA com o score de probabilidade.
  </action>
  <verify>SELECT * FROM unified_client_events WHERE module = 'legal'</verify>
  <done>A view retorna eventos do NIJA (Dossiê, Review, Judge) corretamente.</done>
</task>

## Success Criteria
- [ ] Schema do banco preparado para o NIJA-MAESTRO V2.
- [ ] Versionamento de documentos jurídicos habilitado em nível de banco.
- [ ] Timeline do cliente rastreia o progresso do pipeline inteligente.
